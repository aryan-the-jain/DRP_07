# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)

GET     /support-requests          controllers.SupportRequestController.index
POST    /support-requests          controllers.SupportRequestController.create

GET     /groups/:groupId                           controllers.PeerSupportController.group(groupId: Int)
GET     /groups/:groupId/participants              controllers.PeerSupportController.participants(groupId: Int)
GET     /participants/:participantId               controllers.PeerSupportController.participantInfo(participantId: Int)
GET     /groups/:groupId/messages                  controllers.PeerSupportController.messages(groupId: Int)
POST    /groups/:groupId/messages                  controllers.PeerSupportController.sendGroupMessage(groupId: Int)
POST    /groups/:groupId/facilitator-messages      controllers.PeerSupportController.sendFacilitatorMessage(groupId: Int)
GET     /groups/:groupId/:participantId/facilitator-messages      controllers.PeerSupportController.facilitatorMessages(groupId: Int, participantId: Int)
POST    /groups/:groupId/reflections               controllers.PeerSupportController.createReflection(groupId: Int)
GET     /groups/:groupId/participants/:participantId/reflection   controllers.PeerSupportController.latestReflection(groupId: Int, participantId: Int)
GET     /groups/:groupId/support-links             controllers.PeerSupportController.supportLinks(groupId: Int)
